doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title AI Models | #{title}
    link(rel="icon" href="/img/images/logo.png")
    link(rel="stylesheet" href="/styles/tailwind.css")

  body(class="bg-gray-100 flex items-center justify-center h-screen")
    div(class="w-[70rem] max-w-sm bg-white.p-8 rounded-lg shadow-md p-5")
      block content
        form(action="/users/add-relative" method="POST")
          h1(class="text-2xl font-semibold text-center text-gray-700 mb-6") You Must Enter a Relative Email before Doing Test
          .mb-4
            label(for="email", class="block text-sm font-medium text-gray-600") Email Address
            input(id="email", type="email", name="email", placeholder="Enter Your Relative's Email", class="w-full p-3 mt-1 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500", required)

          button(class="forget-password-btn w-full bg-blue-500 text-white p-3 rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 mb-4", type="submit") Send

          .text-center
            a(href="/home#models", class="text-blue-500 hover:underline") Go back to Tests